#pragma once
#include <vector>
#include <string>
#include <fstream>
#include <iostream>

class ReadFromFileClass
{
protected:
	std::vector <std::string> dataFromFile(std::string& path);
};

